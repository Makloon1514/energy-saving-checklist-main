# 📘 คู่มือการติดตั้งและใช้งานระบบ Energy Saving Checklist (Supabase Backend)

ระบบแบบประเมินประหยัดพลังงานนี้ทำงานโดยใช้:

1. **Frontend (หน้าเว็บ):** ใช้ React (Vite) เพื่อความรวดเร็ว
2. **Backend (ฐานข้อมูล):** เก็บข้อมูลลง Supabase (PostgreSQL) โดยตรงผ่าน `@supabase/supabase-js` ทำให้ไม่ต้องมีเซิร์ฟเวอร์ Backend คั่นกลาง

---

## 🛠 ขั้นตอนที่ 1: การตั้งค่า Backend (Supabase)

1. **สร้าง Project ใน Supabase**
   - ไปที่ [Supabase](https://supabase.com) สร้าง Project ใหม่ หรือใช้โปรเจกต์เดิมที่มีอยู่

2. **สร้างตารางและตั้งค่าสิทธิ์ (SQL Editor)**
   - ไปที่เมนู **SQL Editor** ในหน้าต่าง Supabase
   - กดรูป + สร้าง New Query
   - ก๊อปปี้โค้ดทั้งหมดที่อยู่ในไฟล์ `supabase_schema.sql` ในโปรเจกต์นี้
   - นำไปวางในหน้า SQL Editor แล้วกดปุ่ม **RUN**
   - ระบบจะสร้างตาราง `records` และตั้งค่าการเข้าถึงเพื่อใช้งานให้เรียบร้อย

3. **นำ API Keys มาใส่ในโปรเจกต์**
   - ใน Supabase ไปที่ฟันเฟือง (Project Settings) ทางล่างซ้าย -> เลือกเมนู **API**
   - หาหัวข้อ **Project URL** (คัดลอกเก็บไว้)
   - หาหัวข้อ **Project API keys** ดูบรรทัดชื่อว่า **`anon`** `public` (คัดลอกเก็บไว้)

---

## 💻 ขั้นตอนที่ 2: การเชื่อมต่อ Frontend (React)

1. **ตั้งค่าไฟล์ .env**
   - ภายในโปรเจกต์ (โฟลเดอร์รหัส `quantum-station`) ให้สร้างไฟล์ชื่อว่า `.env`
   - เปิดไฟล์นี้ขึ้นมาและนำ URL กับ KEY ที่คัดลอกมาเมื่อสักครู่ไปใส่ในรูปแบบนี้:
     ```env
     VITE_SUPABASE_URL="https://[YOUR_PROJECT_ID].supabase.co"
     VITE_SUPABASE_ANON_KEY="[YOUR_EYJ_LONG_ANON_KEY]"
     ```
   - กด Save ไฟล์ให้เรียบร้อย

2. **ทดสอบระบบในเครื่องส่วนตัว (Local)**
   - เปิด Terminal ในโปรเจกต์นี้ รันคำสั่งติดตั้งแพ็กเกจ: `npm install`
   - รันคำสั่งเปิดเซิร์ฟเวอร์หน้าเว็บ: `npm run dev`
   - เข้าเว็บผ่านลิงก์ `http://localhost:5173`
   - ทดลองเข้ามากดเซฟข้อมูล หากมีข้อมูลเข้าในเว็บ Supabase (เมนู Table Editor) แสดงว่าใช้งานได้!

---

## 🚀 ขั้นตอนที่ 3: การสั่งทำงานหน้าเว็บสำหรับออนไลน์ (Deploy Frontend)

สามารถฝากเว็บขึ้นที่ [Render.com](https://render.com) ได้เลย ไม่ต้องแก้โค้ดเพิ่ม:

1. นำโค้ดขึ้น GitHub
2. ในเว็บ **Render** สร้าง Web Service เป็น **Static Site**
3. เลือก Repository จาก GitHub
4. สำคัญมาก: ในหน้า Deploy (แถบ Environment Variables) ต้องเพิ่มค่าที่สำหรับต่อ Supabase ให้ครบ:
   - กรอก **VITE_SUPABASE_URL** และใส่ URL ให้ตรงกับใน `.env`
   - กรอก **VITE_SUPABASE_ANON_KEY** และใส่ KEY ให้ตรงกับใน `.env`
5. กำหนดคำสั่ง Build Command: `npm run build`
6. กำหนด Publish directory: `dist`
7. กด Deploy เท่านี้เสร็จสมบูรณ์ 100% ครับ!
